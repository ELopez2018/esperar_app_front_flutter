import 'package:flutter/material.dart';

class DetailCompanyRoute extends StatelessWidget {
  const DetailCompanyRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}